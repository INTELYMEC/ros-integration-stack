version: "3.9"

services:
  robot_p3at_sim:
    build: ./robot_p3at_sim
    networks:
      - rosnet

  ros2_bridge:
    build: ./ros2_bridge
    platform: linux/amd64
    networks:
      - rosnet
    depends_on:
      - robot_p3at_sim

networks:
  rosnet: